import { FenceCasePipe } from './fence-case.pipe';

describe('FenceCasePipe', () => {
  it('create an instance', () => {
    const pipe = new FenceCasePipe();
    expect(pipe).toBeTruthy();
  });
});
