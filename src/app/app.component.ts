import { Component } from '@angular/core';
// import { FormControl, FormGroup, Validators } from '@angular/forms';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss']
})
export class AppComponent {
  title = 'Tout of Heroes';

  // Work 1
  // form = new FormGroup({
  //   temp1: new FormControl(false, Validators.required),
  //   temp2: new FormControl(true, Validators.required),
  //   temp3: new FormControl(true, Validators.required),
  //   temp4: new FormControl(5, [Validators.required ,Validators.email]),
  //   })
}
