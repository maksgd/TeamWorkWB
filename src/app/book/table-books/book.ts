export interface BookElement {
  id: number,
  title: string,
  qtyRelease: number,
  releaseDate: string,
  description: string
}

export interface CartElement {
  id: number,
  title: string,
  description: string,
}